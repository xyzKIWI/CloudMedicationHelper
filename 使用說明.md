# 雲端藥歷整理小幫手 1.2.4－使用說明

## 用途

本擴充功能用於整理健保醫療資訊雲端查詢系統當前頁面已顯示的：

- 用藥紀錄
- 檢驗結果
- 影像及病理報告

結果供授權醫療工作人員快速整理與核對，不取代健保原頁、正式病歷或臨床判斷。

## 安裝

1. 將 ZIP 完整解壓縮。
2. Edge 網址列輸入 `edge://extensions`。
3. 打開「開發人員模式」。
4. 點「載入解壓縮」，選擇解壓縮後的 `extension` 資料夾。
5. 確認擴充功能顯示版本 `1.2.4`。

## 操作

1. 依平常流程登入健保雲端，開啟用藥、檢驗或影像及病理頁面。
2. 若資料有分頁，先切換成顯示全部，並核對總筆數。
3. 按右下角綠色的「整理」按鈕。
4. 用藥與檢驗可選擇對應的複製格式。
5. 影像及病理會自動逐筆讀取原頁「報告」按鈕；完成後按「複製整理表格」。

影像及病理輸出只包含五欄：檢驗日期、醫令名稱、報告結果、檢驗類別、來源。原頁沒有「報告」按鈕的列會列為無法讀取。

## 更新版本

1. 解壓縮新版 ZIP，不要直接覆蓋正在使用的資料夾。
2. 在 `edge://extensions` 移除舊版，或將舊版指向新版 `extension` 資料夾後按「重新載入」。
3. 重新開啟健保雲端頁面，確認右下角出現整理按鈕。

## 隱私與安全

- 擴充功能沒有對外網路連線與上傳功能，也不使用瀏覽器儲存區保存病人資料。
- 資料僅在目前分頁記憶體處理；換頁或換個案時會關閉舊面板。
- 複製結果會進入 Windows 剪貼簿，貼入授權系統後請依院內規範清除。
- 只能用於已授權的臨床或醫療行政工作。

## 常見狀況

- **沒有出現整理按鈕**：確認位於 `medcloud2.nhi.gov.tw/imu/` 且擴充功能已啟用，再重新開啟該頁。
- **筆數不完整**：先將健保原頁分頁切成顯示全部。
- **某份報告讀取失敗**：單筆最多等待約 12 秒後會繼續下一筆；請回原頁手動點該筆「報告」核對。
- **換個案後還看到舊面板**：先關閉面板並重新開啟當前頁；不要使用舊面板內容。
