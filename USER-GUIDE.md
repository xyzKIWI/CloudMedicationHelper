# 雲端藥歷整理小幫手 1.3.7－使用說明

## 用途

本擴充功能用於整理健保醫療資訊雲端查詢系統當前頁面已顯示的：

- 用藥紀錄
- 檢驗結果
- 影像及病理報告
- 手術紀錄
- 出院診斷
- 過敏紀錄（重複藥物合併）

結果供授權醫療工作人員快速整理與核對，不取代健保原頁、正式病歷或臨床判斷。

## 安裝

1. 將 ZIP 完整解壓縮。
2. Edge 網址列輸入 `edge://extensions`。
3. 打開「開發人員模式」。
4. 點「載入解壓縮」，選擇解壓縮後的 `extension` 資料夾。
5. 確認擴充功能顯示版本 `1.3.7`。

## 操作

### 整合病歷摘要

1. 依平常流程登入健保雲端並完成查詢；各頁若有分頁，先切成顯示全部並核對總筆數。
2. 回到「摘要」頁，按右下角綠色的「彙整病歷」。
3. 程式會依序切換西醫用藥、檢驗、影像及病理、手術、出院病摘與過敏紀錄頁籤。
4. 影像及病理只讀最近三個曆月內的報告結果；出院病摘會逐筆開啟原頁明細，並只將「出院診斷」放入摘要。
5. 完成後程式回到摘要頁。先核對六區狀態與預覽，再按「複製完整病歷摘要」。

出院診斷擷取期間，程式會保留並重用健保雲端另開的 ShowXml 明細視窗；彙整完成後可自行關閉該視窗。
整合摘要固定在畫面右側、從「摘要」導覽列下方開始，不會遮住頁首身分證號與換病患操作區；按標題列的「—」可收合成小標題列，再按「□」即可展開，不會清除已整理內容。

近三個月採曆月計算，起訖日皆包含；西醫用藥、檢驗、影像及病理套用此範圍。手術、出院病摘與過敏紀錄依健保原頁目前查詢範圍。

### 單頁整理

在西醫用藥、檢驗或影像及病理頁按右下角按鈕，可繼續使用原本的精簡藥歷、檢驗條列與報告表格功能。精簡藥歷複製及整合病歷摘要的藥歷區塊使用「成分名稱（學名）」；若原頁成分欄空白，會明確顯示「未對應成分」。影像及病理單頁整理亦只納入最近三個月。

影像及病理輸出只包含五欄：檢驗日期、醫令名稱、報告結果、檢驗類別、來源。只有「影像查詢」且報告結果欄空白的列不會納入；有「報告」按鈕但讀取失敗的列才會列為無法讀取。

過敏紀錄優先依藥品代碼去重；沒有代碼時才依正規化藥名合併。空白結果不能當作 NKDA，必須回原頁及問診核對。

## 更新版本

1. 解壓縮新版 ZIP，不要直接覆蓋正在使用的資料夾。
2. 在 `edge://extensions` 移除舊版，或將舊版指向新版 `extension` 資料夾後按「重新載入」。
3. 重新開啟健保雲端頁面，確認右下角出現整理按鈕。

## 隱私與安全

- 擴充功能沒有對外網路連線與上傳功能，也不使用瀏覽器儲存區保存病人資料。
- 資料僅在目前分頁記憶體處理；單頁整理遇到表格變更會關閉舊面板，整合流程完成或關閉分頁後不會持久保存。
- 整合流程只保留記憶體內病人識別指紋；流程中若識別狀態改變，或開始新查詢／登出，會立即中止並清除本次結果。
- 複製結果會進入 Windows 剪貼簿，貼入授權系統後請依院內規範清除。
- 只能用於已授權的臨床或醫療行政工作。

## 常見狀況

- **沒有出現整理按鈕**：確認位於 `medcloud2.nhi.gov.tw/imu/` 且擴充功能已啟用，再重新開啟該頁。
- **筆數不完整**：先將健保原頁分頁切成顯示全部。
- **某份報告讀取失敗**：單筆最多等待約 12 秒後會繼續下一筆；請回原頁手動點該筆「報告」核對。
- **某區顯示未完成**：可能是該頁籤不可用、欄位改版或表格尚未載入；請回該頁核對，不要把未完成狀態解讀成無資料。
- **無法啟動整合摘要**：確認頁首已顯示遮罩後的身分證號；為避免跨病人資料混合，無法辨識時不會啟動。
- **換個案後還看到舊面板**：先關閉面板並重新開啟當前頁；不要使用舊面板內容。
